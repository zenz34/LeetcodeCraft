By looking at the O(nlogN) I see that must be a merge sort
my mergesort thoughts
        /*
            end condition:
            only single one return
            null return
            
        
        
            ListNode lHead = softList(leftPart);
            ListNode rHead = softList(rightPart);
            mergeSort(lHead, rHead);
        */


